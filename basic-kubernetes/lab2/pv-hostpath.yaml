apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-hostpath
  namespace: lab2
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
